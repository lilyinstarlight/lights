[package]
name = "fooster_lights"
version = "0.1.0"
authors = ["Lily Foster <lily@lily.flowers>"]
edition = "2018"
publish = false

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
futures-util = "^0.3"
rocket = "^0.4"
rocket_contrib = { version = "^0.4", features = ["json", "tera_templates"] }
rosc = "^0.4"
rppal = "^0.12"
serde = { version = "^1.0", features = ["derive"] }
serde_json = "^1.0"
serde_with = "^1.7"
tokio = { version = "^1.4", features = ["macros", "net", "rt", "time"] }
tokio-tungstenite = "^0.13"
yansi = "^0.5"
